{
  "name": "nuxt-express",
  "version": "1.0.0",
  "private": true,
  "main":"./build/dist/server.js",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "ls -la  && ./node_modules/.bin/nuxt start",
    "generate": "nuxt generate",
    "lint": "yarn lint:js",
    "lint:js": "eslint --ext .js,.vue --ignore-path .gitignore ."
  },
  "dependencies": {
    "@nuxt/http": "latest",
    "express": "latest",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
  }
}
